# ⚠️ CONFIGURAÇÃO DE EXEMPLO DO FRONTEND - NÃO PREENCHA COM DADOS REAIS
# Este arquivo é apenas um template. Para executar a aplicação:
# 1. Copie este arquivo: cp .env.example .env.local
# 2. Preencha os valores com sua configuração local
# 3. NUNCA faça commit do arquivo .env.local

# ═══════════════════════════════════════════════════════════════════════════
# 🌐 CONFIGURAÇÃO DA API (Backend)
# ═══════════════════════════════════════════════════════════════════════════
# URL base para requisições à API do backend
# Desenvolvimento Local: http://localhost:3000/api
# Produção: https://seu-dominio.com/api
API_BASE_URL=http://localhost:3000/api

# ═══════════════════════════════════════════════════════════════════════════
# 📝 INSTRUÇÕES DE SEGURANÇA
# ═══════════════════════════════════════════════════════════════════════════
#
# ⚠️  IMPORTANTE:
# - Arquivos .env.local NUNCA devem ser comitados no Git
# - Verifique se .env.local está no .gitignore (já está configurado)
# - Em produção, use variáveis de ambiente do servidor
# - Não exponha URLs de staging/produção em repositórios públicos
#
# ✅ VERIFICAÇÃO:
# 1. Certifique-se de que .env* está em .gitignore
#    grep "\.env" .gitignore
# 2. Se já comitou acidentalmente:
#    git rm --cached .env.local
#    git commit -m "Remove .env.local file"
#
# 🔄 ALTERNATIVAS DE CONFIGURAÇÃO:
# 1. app.json - Configurações públicas (comitáveis)
# 2. .env.local - Configurações locais (não comitáveis)
# 3. Variáveis de ambiente do sistema
#    export API_BASE_URL=http://seu-ip:3000/api
